import XCTest
@testable import NsUpdateScreenModule

final class NsUpdateScreenModuleTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NsUpdateScreenModule().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
